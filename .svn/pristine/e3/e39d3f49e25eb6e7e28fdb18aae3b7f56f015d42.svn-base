import { TestBed, inject } from '@angular/core/testing';
import { PravilaService } from './pravila.service';

describe('PravilaService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [PravilaService]
    });
  });

  it('should be created', inject([PravilaService], (service: PravilaService) => {
    expect(service).toBeTruthy();
  }));
});
