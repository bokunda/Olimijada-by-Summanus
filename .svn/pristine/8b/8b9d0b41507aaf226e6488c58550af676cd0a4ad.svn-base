import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { MeceviPoMesecimaComponent } from './mecevi-po-mesecima.component';

describe('MeceviPoMesecimaComponent', () => {
  let component: MeceviPoMesecimaComponent;
  let fixture: ComponentFixture<MeceviPoMesecimaComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ MeceviPoMesecimaComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(MeceviPoMesecimaComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
