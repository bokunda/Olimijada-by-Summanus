import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { MeceviTurniraComponent } from './mecevi-turnira.component';

describe('MeceviTurniraComponent', () => {
  let component: MeceviTurniraComponent;
  let fixture: ComponentFixture<MeceviTurniraComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ MeceviTurniraComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(MeceviTurniraComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
