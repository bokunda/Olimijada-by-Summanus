import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PrikazBotovaComponent } from './prikaz-botova.component';

describe('PrikazBotovaComponent', () => {
  let component: PrikazBotovaComponent;
  let fixture: ComponentFixture<PrikazBotovaComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PrikazBotovaComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PrikazBotovaComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
